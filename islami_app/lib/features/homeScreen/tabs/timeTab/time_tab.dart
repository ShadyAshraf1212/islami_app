import 'package:flutter/material.dart';

class TimeTab extends StatelessWidget {
  TimeTab({super.key});
  static String routeName = 'timeTab';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
